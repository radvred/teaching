# 1. Базовый образ: берем официальный легкий образ Python.
# Alpine - очень маленький и быстрый Linux.
FROM python:3.11-alpine

# 2. Указываем рабочую директорию внутри контейнера.
# Все команды дальше будут выполняться в этой папке.
WORKDIR /app

# 3. Копируем файл с зависимостями (с хоста в контейнер).
# Сначала копируем ТОЛЬКО requirements.txt. Это делается для кэширования.
COPY requirements.txt .

# 4. Устанавливаем зависимости внутри контейнера.
RUN pip install --no-cache-dir -r requirements.txt

# 5. Копируем остальной код приложения.
COPY app.py .

# 6. Указываем, что контейнер будет слушать порт 5000.
EXPOSE 5000

# 7. Команда, которая запустится при старте контейнера.
CMD ["python", "app.py"]
